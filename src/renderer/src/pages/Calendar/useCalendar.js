// Event
/*
{
  id: number,
  title: string,
  description: string,
  start: string,           // ISO datetime
  end: string,             // ISO datetime
  color: string,           // mantine color string
  allDay: boolean,
  recurring: boolean,
  frequency: 'daily' | 'weekly' | 'monthly' | null,
  taskId: number | null,   // if created from a task deadline
  labelId: string | null,  // future: category/label system
}*/


// Label (future — modular, not built now)
// {
//   id: string,
//   name: string,
//   color: string,
// }